let score = 100;
//console.log(score);

let balance = new Number(2000);
// console.log(balance);
// console.log(balance.toString());

// console.log(balance.toString().length);

// console.log(balance.toFixed(2));

const hunders = 10000000;
// console.log(hunders.toLocaleString('en-IN'));



        // ++++++++++++++Maths+++++++++++++++ 



/*
console.log(Math.PI); // Returns the value of PI

console.log(Math.abs(-10)); // Returns the absolute value

console.log(Math.round(4.5)); // Rounds to the nearest integer

console.log(Math.ceil(4.5));  // Rounds up to the nearest integer

console.log(Math.floor(4.5)); // Rounds down to the nearest integer

console.log(Math.min(10, 20, 30, 40)); // Returns the minimum value

console.log(Math.max(10, 20, 30, 40)); // Returns the maximum value

console.log(Math.random()); // Returns a random number between 0 and 1

console.log(Math.sqrt(16)); // Returns the square root of a number

console.log(Math.pow(2, 3)); // Returns the power of a number (2^3 = 8)

console.log(Math.floor(Math.random() * 10)); // Random integer between 0 and 10 */

console.log(Math.floor(Math.random() * 10) + 1); // Random integer between 1 and 10 

console.log(Math.floor(Math.random() * 100) + 1); // Random integer between 1 and 100 

const min =10;
const max = 20;

console.log(Math.floor(Math.random() * (max - min + 1)) + min); // Random integer between 10 and 20







